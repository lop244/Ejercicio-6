/**
 * Clase Principal (Driver).
 * Su única responsabilidad es instanciar e iniciar el Controlador.
 */
public class Main {
    
    public static void main(String[] args) {
        ControladorPrincipal controlador = new ControladorPrincipal();
        controlador.run();
    }
}